name: Moodle Plugin CI
on: [push, pull_request]
jobs:
  ci:
    uses: bycs-lp/moodle-ci-workflows/.github/workflows/moodle-plugin-ci.yml@main
    with:
      extra-plugins: |
        bycs-lp/moodle-local_ai_content
